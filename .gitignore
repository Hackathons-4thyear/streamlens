# Secrets
.env
.env.local
*.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.db
*.sqlite3
api/uploads/

# Node
node_modules/
dist/
dist-ssr/
.vite/
coverage/
*.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Evaluation images: keep third-party licensing out of the repository.
# The reports and labels ARE committed; the photographs are not.
eval/photos/*
!eval/photos/README.md
!eval/photos/.gitkeep
eval/_dryrun_photos/

# Source documents fetched from their publishers. Cited, not redistributed:
# see data/README.md for the DOI and the download command.
data/sources/

# Downloaded tooling: the FHIR validator jar (200 MB) and the IG source.
# docs/fhir/validation-report.md has the commands to fetch both.
tools/

# TypeScript incremental build state, regenerated on every build
web/tsconfig.tsbuildinfo
